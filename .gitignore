# ── Sensitive / internal planning docs — NEVER push these ──
DONOTPUSHTOGITHUB/
DEVKARM_MASTERPLAN.md
DEVKARM_ENTERPRISE_PLAYBOOK.md
DEVKARM_PLAYBOOK_PATCHES.md

# ── Build outputs ──
node_modules/
target/
dist/
*.wasm

# ── WASM binaries in public/ — copied from node_modules by postinstall script ──
# Never commit these. After cloning: cd apps/web && npm install (runs copy-wasm.js automatically)
apps/web/public/tree-sitter.wasm
apps/web/public/tree-sitter-javascript.wasm

# ── Environment / secrets ──
.env
apps/api/.env
apps/web/.env

# ── Rust build cache ──
apps/api/target/

# ── OS junk ──
.DS_Store
Thumbs.db
